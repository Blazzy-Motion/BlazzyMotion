name: 📦 Publish NuGet

on:
  workflow_dispatch:
    inputs:
      core_version:
        description: "Core Package Version (e.g., 1.0.0-preview1)"
        required: true
        default: "1.0.0-preview1"
        type: string
      carousel_version:
        description: "Carousel Package Version (e.g., 1.1.0-preview1)"
        required: true
        default: "1.1.0-preview1"
        type: string
      publish_core:
        description: "Publish Core package?"
        required: true
        default: true
        type: boolean
      publish_carousel:
        description: "Publish Carousel package?"
        required: true
        default: true
        type: boolean

env:
  DOTNET_VERSION: "8.0.x"
  CORE_PROJECT: "src/BlazzyMotion.Core/BlazzyMotion.Core.csproj"
  CAROUSEL_PROJECT: "src/BlazzyMotion.Carousel/BlazzyMotion.Carousel.csproj"

jobs:
  publish:
    name: Pack & Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write

    env:
      CORE_VERSION: ${{ inputs.core_version }}
      CAROUSEL_VERSION: ${{ inputs.carousel_version }}
      PUBLISH_CORE: ${{ inputs.publish_core }}
      PUBLISH_CAROUSEL: ${{ inputs.publish_carousel }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build Solution (Release)
        run: dotnet build --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test --configuration Release --no-build --verbosity normal

      - name: Pack BlazzyMotion.Core
        if: ${{ inputs.publish_core }}
        run: |
          dotnet pack ${{ env.CORE_PROJECT }} \
            --configuration Release \
            --no-build \
            --output ./nupkgs \
            /p:PackageVersion="$CORE_VERSION"

      - name: Pack BlazzyMotion.Carousel
        if: ${{ inputs.publish_carousel }}
        run: |
          dotnet pack ${{ env.CAROUSEL_PROJECT }} \
            --configuration Release \
            --no-build \
            --output ./nupkgs \
            /p:PackageVersion="$CAROUSEL_VERSION"

      - name: List Packages
        run: ls -lah ./nupkgs

      - name: Push to NuGet.org
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          for package in ./nupkgs/*.nupkg; do
            echo "Publishing: $package"
            dotnet nuget push "$package" \
              --api-key "$NUGET_API_KEY" \
              --source https://api.nuget.org/v3/index.json \
              --skip-duplicate
          done

      - name: Create Git Tags
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ "$PUBLISH_CORE" = "true" ]; then
            git tag -a "core-v$CORE_VERSION" -m "Release BlazzyMotion.Core v$CORE_VERSION"
            git push origin "core-v$CORE_VERSION"
            echo "Created tag: core-v$CORE_VERSION"
          fi

          if [ "$PUBLISH_CAROUSEL" = "true" ]; then
            git tag -a "carousel-v$CAROUSEL_VERSION" -m "Release BlazzyMotion.Carousel v$CAROUSEL_VERSION"
            git push origin "carousel-v$CAROUSEL_VERSION"
            echo "Created tag: carousel-v$CAROUSEL_VERSION"
          fi

      - name: Publication Summary
        run: |
          echo "### 🎉 NuGet Packages Published!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$PUBLISH_CORE" = "true" ]; then
            echo "#### BlazzyMotion.Core" >> $GITHUB_STEP_SUMMARY
            echo "- **Version:** $CORE_VERSION" >> $GITHUB_STEP_SUMMARY
            echo "- **NuGet:** https://www.nuget.org/packages/BlazzyMotion.Core/$CORE_VERSION" >> $GITHUB_STEP_SUMMARY
            echo "- **Tag:** \`core-v$CORE_VERSION\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "$PUBLISH_CAROUSEL" = "true" ]; then
            echo "#### BlazzyMotion.Carousel" >> $GITHUB_STEP_SUMMARY
            echo "- **Version:** $CAROUSEL_VERSION" >> $GITHUB_STEP_SUMMARY
            echo "- **NuGet:** https://www.nuget.org/packages/BlazzyMotion.Carousel/$CAROUSEL_VERSION" >> $GITHUB_STEP_SUMMARY
            echo "- **Tag:** \`carousel-v$CAROUSEL_VERSION\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 Installation" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          if [ "$PUBLISH_CAROUSEL" = "true" ]; then
            echo "# Install Carousel (automatically installs Core)" >> $GITHUB_STEP_SUMMARY
            echo "dotnet add package BlazzyMotion.Carousel --version $CAROUSEL_VERSION" >> $GITHUB_STEP_SUMMARY
          elif [ "$PUBLISH_CORE" = "true" ]; then
            echo "# Install Core only" >> $GITHUB_STEP_SUMMARY
            echo "dotnet add package BlazzyMotion.Core --version $CORE_VERSION" >> $GITHUB_STEP_SUMMARY
          fi
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### 📦 Package Contents" >> $GITHUB_STEP_SUMMARY
          if [ "$PUBLISH_CORE" = "true" ]; then
            echo "**BlazzyMotion.Core:**" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzComponentBase (abstract base class)" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzImage, BzTitle, BzDescription attributes" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzRegistry (type mapping service)" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzTheme enum (Glass, Dark, Light, Minimal)" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzTemplateFactory" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "$PUBLISH_CAROUSEL" = "true" ]; then
            echo "**BlazzyMotion.Carousel:**" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ BzCarousel<T> component" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Source Generator (bundled)" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ CSS and JS assets (Swiper.js)" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ 3D Coverflow effect" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Glassmorphism themes" >> $GITHUB_STEP_SUMMARY
          fi
